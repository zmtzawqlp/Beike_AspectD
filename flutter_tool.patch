diff --git a/packages/flutter_tools/lib/src/build_system/targets/common.dart b/packages/flutter_tools/lib/src/build_system/targets/common.dart
index 1be5f6aff5a..7b4eb00676c 100644
--- a/packages/flutter_tools/lib/src/build_system/targets/common.dart
+++ b/packages/flutter_tools/lib/src/build_system/targets/common.dart
@@ -13,6 +13,7 @@ import '../../compile.dart';
 import '../../dart/package_map.dart';
 import '../../darwin/darwin.dart';
 import '../../devfs.dart';
+import '../../aop/aspectd.dart';
 import '../../globals.dart' as globals show xcode;
 import '../../project.dart';
 import '../build_system.dart';
@@ -165,6 +166,11 @@ class KernelSnapshot extends Target {
 
   @override
   Future<void> build(Environment environment) async {
+    await AspectdHook.enableAspectd();
+    await buildImpl(environment);
+  }
+
+  Future<CompilerOutput> buildImpl(Environment environment) async {
     final compiler = KernelCompiler(
       fileSystem: environment.fileSystem,
       logger: environment.logger,
@@ -281,6 +287,8 @@ class KernelSnapshot extends Target {
     if (output == null || output.errorCount != 0) {
       throw Exception();
     }
+
+    return output;
   }
 
   Future<void> _addFlavorToDartDefines(
diff --git a/packages/flutter_tools/lib/src/build_system/targets/web.dart b/packages/flutter_tools/lib/src/build_system/targets/web.dart
index f3a8d036475..51dee017099 100644
--- a/packages/flutter_tools/lib/src/build_system/targets/web.dart
+++ b/packages/flutter_tools/lib/src/build_system/targets/web.dart
@@ -16,6 +16,7 @@ import '../../cache.dart';
 import '../../convert.dart';
 import '../../dart/language_version.dart';
 import '../../dart/package_map.dart';
+import '../../aop/aop_manager.dart';
 import '../../flutter_plugins.dart';
 import '../../globals.dart' as globals;
 import '../../project.dart';
@@ -202,6 +203,10 @@ class Dart2JSTarget extends Dart2WebTarget {
     // Run the dart2js compilation in two stages, so that icon tree shaking can
     // parse the kernel file for web builds.
     await processUtils.run(cfeCompilationArgs, throwOnError: true);
+    await AopManager.hookSnapshotCommand(
+      environment.buildDir.childFile('app.dill').path,
+      buildMode,
+    );
 
     final File outputJSFile = environment.buildDir.childFile('main.dart.js');
 
diff --git a/packages/flutter_tools/lib/src/commands/build_bundle.dart b/packages/flutter_tools/lib/src/commands/build_bundle.dart
index e2252f23c99..a94186b3cb2 100644
--- a/packages/flutter_tools/lib/src/commands/build_bundle.dart
+++ b/packages/flutter_tools/lib/src/commands/build_bundle.dart
@@ -11,6 +11,7 @@ import '../bundle_builder.dart';
 import '../features.dart';
 import '../globals.dart' as globals;
 import '../project.dart';
+import '../aop/aspectd.dart';
 import '../runner/flutter_command.dart';
 import 'build.dart';
 
@@ -107,6 +108,8 @@ class BuildBundleCommand extends BuildSubCommand {
 
   @override
   Future<FlutterCommandResult> runCommand() async {
+    await AspectdHook.enableAspectd();
+
     final String targetPlatform = stringArg('target-platform')!;
     final TargetPlatform platform = getTargetPlatformForName(targetPlatform);
     // Check for target platforms that are only allowed via feature flags.
diff --git a/packages/flutter_tools/lib/src/compile.dart b/packages/flutter_tools/lib/src/compile.dart
index ca04cfb345a..d9a20bf910f 100644
--- a/packages/flutter_tools/lib/src/compile.dart
+++ b/packages/flutter_tools/lib/src/compile.dart
@@ -16,6 +16,7 @@ import 'base/file_system.dart';
 import 'base/io.dart';
 import 'base/logger.dart';
 import 'base/platform.dart';
+import 'aop/aspectd.dart';
 import 'build_info.dart';
 import 'convert.dart';
 
@@ -255,6 +256,9 @@ class KernelCompiler {
     required PackageConfig packageConfig,
     String? nativeAssets,
   }) async {
+    await AspectdHook.enableAspectd();
+    final bool aopConfig = AspectdHook.configFileExists();
+
     final TargetPlatform? platform = targetModel == TargetModel.dartdevc
         ? TargetPlatform.web_javascript
         : null;
@@ -358,6 +362,7 @@ class KernelCompiler {
             '--initialize-from-dill',
             initializeFromDill,
           ],
+          if (aopConfig) ...<String>['--aop', '1'],
           if (platformDill != null) ...<String>['--platform', platformDill],
           if (dartPluginRegistrantUri != null) ...<String>[
             '--source',
@@ -374,6 +379,9 @@ class KernelCompiler {
         ];
 
     _logger.printTrace(command.join(' '));
+    if (buildMode == BuildMode.debug) {
+      _logger.printStatus('Debug frontend_server start args: ${command.skip(1).join(' ')}');
+    }
     final Process server = await _processManager.start(command);
 
     server.stderr.transform<String>(utf8.decoder).listen(_logger.printError);
@@ -819,6 +827,9 @@ class DefaultResidentCompiler implements ResidentCompiler {
     String? additionalSourceUri,
     String? nativeAssetsUri,
   }) async {
+    await AspectdHook.enableAspectd();
+    final bool aopConfig = AspectdHook.configFileExists();
+
     final TargetPlatform? platform = (targetModel == TargetModel.dartdevc)
         ? TargetPlatform.web_javascript
         : null;
@@ -877,12 +888,16 @@ class DefaultResidentCompiler implements ResidentCompiler {
           ],
           if (nativeAssetsUri != null) ...<String>['--native-assets', nativeAssetsUri],
           if (platformDill != null) ...<String>['--platform', platformDill!],
+          if (aopConfig) ...<String>['--aop', '1'],
           if (unsafePackageSerialization) '--unsafe-package-serialization',
           // See: https://github.com/flutter/flutter/issues/103994
           '--verbosity=error',
           ...?extraFrontEndOptions,
         ];
     _logger.printTrace(command.join(' '));
+    if (buildMode == BuildMode.debug) {
+      _logger.printStatus('Debug frontend_server start args: ${command.skip(1).join(' ')}');
+    }
     _server = await _processManager.start(command);
     _server?.stdout
         .transform<String>(utf8.decoder)
